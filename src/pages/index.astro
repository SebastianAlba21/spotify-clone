---
import { playlists } from '@/lib/data'
import Layout from '@/layouts/Layout.astro'
import PlayListItemCard from '@/components/PlayListItemCard.astro'
import SpotifyLogo from '@/icons/Spotify.astro'
---

<Layout title="Spotify">
	<div
		id="playlist-container"
		class="relative transition duration-1000 bg-green-600 text-white">
		<div class="relative z-10 px-6 pt-10">
			<SpotifyLogo/>
			<div class="flex flex-wrap mt-6 gap-4">
				{playlists.map(playlist => <PlayListItemCard playlist={playlist} />)}
			</div>
		</div>
		<div
			id="playlist-background"
			class="absolute inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/80">
		</div>
	</div>
</Layout>

<style></style>
